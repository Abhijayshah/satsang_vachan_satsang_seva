let voicesCache=[];function getPrefs(){try{const raw=localStorage.getItem('tts:prefs');if(!raw)return{rate:.95,pitch:1};const p=JSON.parse(raw);return Object.assign({rate:.95,pitch:1},p)}catch{return{rate:.95,pitch:1}}}function setPrefs(p){const merged=Object.assign(getPrefs(),p||{});localStorage.setItem('tts:prefs',JSON.stringify(merged))}
function setLang(lang){const buttons=document.querySelectorAll('.lang-btn');buttons.forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.langSelect===lang)));const hiEls=document.querySelectorAll('[data-lang="hi"]');const enEls=document.querySelectorAll('[data-lang="en"]');if(lang==='both'){hiEls.forEach(e=>e.style.display='');enEls.forEach(e=>e.style.display='')}if(lang==='hi'){hiEls.forEach(e=>e.style.display='');enEls.forEach(e=>e.style.display='none')}if(lang==='en'){hiEls.forEach(e=>e.style.display='none');enEls.forEach(e=>e.style.display='')}}
function initLangToggle(){document.querySelectorAll('.lang-btn').forEach(btn=>{const run=()=>setLang(btn.dataset.langSelect);btn.addEventListener('pointerdown',run);btn.addEventListener('click',run)});setLang('hi')}
function sanitizeText(t){return t.replace(/[\u0000-\u001F\u007F]/g,' ').trim()}
async function webSpeechSpeak(text,langHint){const prefs=getPrefs();const utter=new SpeechSynthesisUtterance(sanitizeText(text));let langCode=langHint==='en'?'en-IN':'hi-IN';if(langHint==='both')langCode='hi-IN';const voices=voicesCache.length?voicesCache:speechSynthesis.getVoices();const desiredName=langHint==='en'?prefs.voiceEn:prefs.voiceHi;let match;if(desiredName)match=voices.find(v=>v.name===desiredName);if(!match)match=voices.find(v=>v.lang.toLowerCase().startsWith(langCode.toLowerCase()));if(match)utter.voice=match;utter.lang=(match&&match.lang)||langCode;utter.rate=Math.min(1.4,Math.max(.6,prefs.rate));utter.pitch=Math.min(1.3,Math.max(.7,prefs.pitch));speechSynthesis.cancel();speechSynthesis.speak(utter)}
async function remoteSpeak(text,langHint){try{const base=window.__API_BASE||'';const url=(base?base.replace(/\/$/,''):'')+'/api/tts';const res=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({text,lang:langHint})});if(!res.ok)return false;const blob=await res.blob();const objUrl=URL.createObjectURL(blob);const audio=new Audio(objUrl);await audio.play();return true}catch{return false}}
function initTTS(){document.querySelectorAll('.tts-btn').forEach(btn=>{const run=()=>{const card=btn.closest('.card');const currentLangBtn=document.querySelector('.lang-btn[aria-pressed="true"]');const langSel=(currentLangBtn&&currentLangBtn.dataset.langSelect)||'hi';const block=langSel==='en'?card&&card.querySelector('.content[data-lang="en"]'):card&&card.querySelector('.content[data-lang="hi"]');const text=(block&&block.innerText)||(card&&card.innerText)||'';remoteSpeak(text,langSel).then(done=>{if(!done)webSpeechSpeak(text,langSel)})};btn.addEventListener('pointerdown',run);btn.addEventListener('click',run)})}
function createYouTubeEmbed(el,id){document.querySelectorAll('iframe').forEach(f=>{try{f.contentWindow.postMessage('{"event":"command","func":"pauseVideo","args":""}','*')}catch(e){}});const iframe=document.createElement('iframe');iframe.width='100%';iframe.height='100%';iframe.src=`https://www.youtube.com/embed/${encodeURIComponent(id)}?rel=0&modestbranding=1&autoplay=1&playsinline=1&enablejsapi=1`;iframe.allow='accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share; fullscreen';iframe.setAttribute('allowfullscreen','');iframe.allowFullscreen=true;iframe.referrerPolicy='strict-origin-when-cross-origin';iframe.loading='lazy';iframe.title='YouTube video';iframe.style.border='none';el.appendChild(iframe)}
function createImage(el,src){const img=new Image();img.src=src;img.alt='Related image';img.loading='lazy';img.style.width='100%';img.style.height='100%';img.style.objectFit='cover';el.appendChild(img)}
function injectVideoSchema(id,title){const s=document.createElement('script');s.type='application/ld+json';s.textContent=JSON.stringify({"@context":"https://schema.org","@type":"VideoObject","name":title||"Satsang Video","description":"Baba Jai Gurudev Satsang Video - Spiritual Inspiration","thumbnailUrl":`https://img.youtube.com/vi/${id}/maxresdefault.jpg`,"uploadDate":new Date().toISOString(),"embedUrl":`https://www.youtube.com/embed/${id}`});document.head.appendChild(s)}
function initMedia(){document.querySelectorAll('.media').forEach(el=>{const yt=el.getAttribute('data-youtube-id');const img=el.getAttribute('data-image');if(yt){createImage(el,`https://img.youtube.com/vi/${encodeURIComponent(yt)}/hqdefault.jpg`);const btn=document.createElement('div');btn.className='play-button';btn.textContent='Jay Guru Dev Play';el.appendChild(btn);el.addEventListener('click',()=>{el.innerHTML='';createYouTubeEmbed(el,yt);injectVideoSchema(yt,'Satsang Video')})}else if(img){createImage(el,img)}});document.querySelectorAll('.video-card[data-youtube-id]').forEach(c=>{const yt=c.getAttribute('data-youtube-id');const t=c.querySelector('.thumbnail-wrapper');const title=c.querySelector('.video-title')?.textContent;if(yt&&t){t.addEventListener('click',()=>{t.innerHTML='';createYouTubeEmbed(t,yt);t.classList.add('fade-in');injectVideoSchema(yt,title)})}})}
function debounce(fn,delay=200){let t;return(...args)=>{window.clearTimeout(t);t=window.setTimeout(()=>fn(...args),delay)}}
function initSearch(){const input=document.getElementById('searchInput');const cards=Array.from(document.querySelectorAll('.card'));const run=()=>{const q=(input&&input.value||'').toLowerCase().trim();cards.forEach(c=>{const text=(c.innerText||'').toLowerCase();c.style.display=q&&!text.includes(q)?'none':'';const titles=Array.from(c.querySelectorAll('.title'));titles.forEach(t=>{const orig=t.dataset.orig||t.textContent||'';if(!t.dataset.orig)t.dataset.orig=orig;if(!q){t.textContent=orig;return}const idx=orig.toLowerCase().indexOf(q);if(idx>=0){const before=orig.slice(0,idx),match=orig.slice(idx,idx+q.length),after=orig.slice(idx+q.length);t.innerHTML=`${before}<mark>${match}</mark>${after}`}else{t.textContent=orig}})})};if(input){input.addEventListener('input',debounce(run,150));run()}}
function populateVoices(){voicesCache=speechSynthesis.getVoices();const hiSel=document.getElementById('voiceHi');const enSel=document.getElementById('voiceEn');if(!hiSel||!enSel)return;const prefs=getPrefs();const hiVoices=voicesCache.filter(v=>v.lang.toLowerCase().startsWith('hi'));const enVoices=voicesCache.filter(v=>v.lang.toLowerCase().startsWith('en'));const fill=(sel,list,preferred)=>{sel.innerHTML='';list.forEach(v=>{const opt=document.createElement('option');opt.value=v.name;opt.textContent=`${v.name} (${v.lang})`;if(preferred&&v.name===preferred)opt.selected=true;sel.appendChild(opt)})};fill(hiSel,hiVoices,prefs.voiceHi);fill(enSel,enVoices,prefs.voiceEn)}
function initSettings(){const toggle=document.querySelector('.settings-toggle');const panel=document.getElementById('settingsPanel');const rate=document.getElementById('rate');const pitch=document.getElementById('pitch');const rateVal=document.getElementById('rateVal');const pitchVal=document.getElementById('pitchVal');const voiceHi=document.getElementById('voiceHi');const voiceEn=document.getElementById('voiceEn');const testHi=document.getElementById('testHi');const testEn=document.getElementById('testEn');const prefs=getPrefs();if(rate&&rateVal){rate.value=String(prefs.rate);rateVal.textContent=String(prefs.rate)}if(pitch&&pitchVal){pitch.value=String(prefs.pitch);pitchVal.textContent=String(prefs.pitch)}toggle&&toggle.addEventListener('click',()=>{const expanded=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!expanded));if(panel)panel.hidden=expanded});rate&&rate.addEventListener('input',()=>{setPrefs({rate:Number(rate.value)});rateVal&&(rateVal.textContent=String(rate.value))});pitch&&pitch.addEventListener('input',()=>{setPrefs({pitch:Number(pitch.value)});pitchVal&&(pitchVal.textContent=String(pitch.value))});voiceHi&&voiceHi.addEventListener('change',()=>setPrefs({voiceHi:voiceHi.value}));voiceEn&&voiceEn.addEventListener('change',()=>setPrefs({voiceEn:voiceEn.value}));testHi&&testHi.addEventListener('click',()=>webSpeechSpeak('नमस्ते, यह परीक्षण है।','hi'));testEn&&testEn.addEventListener('click',()=>webSpeechSpeak('Hello, this is a test.','en'));populateVoices();if('onvoiceschanged'in speechSynthesis)speechSynthesis.onvoiceschanged=populateVoices}
function openContactMenu(href){const backdrop=document.createElement('div');backdrop.className='contact-menu-backdrop';const panel=document.createElement('div');panel.className='contact-menu';const title=document.createElement('h3');title.textContent='सन्देश चुनें';const actions=document.createElement('div');actions.className='contact-menu-actions';const opts=[
  'Namaste, I want to know more about Satsang Vachan Satsang Seva.',
  'Hello, I want to join the weekly पत्रिका.',
  'I have a query regarding satsang content.'
];opts.forEach(t=>{const b=document.createElement('button');b.type='button';b.textContent=t;b.addEventListener('click',()=>{const url=href.split('?')[0]+'?text='+encodeURIComponent(t);window.open(url,'_blank');document.body.removeChild(backdrop)});actions.appendChild(b)});const cancel=document.createElement('button');cancel.type='button';cancel.className='cancel';cancel.textContent='Cancel';cancel.addEventListener('click',()=>{document.body.removeChild(backdrop)});actions.appendChild(cancel);panel.appendChild(title);panel.appendChild(actions);backdrop.appendChild(panel);backdrop.addEventListener('click',e=>{if(e.target===backdrop)document.body.removeChild(backdrop)});document.body.appendChild(backdrop)}
function initContact(){document.querySelectorAll('a.contact').forEach(a=>{const href=a.getAttribute('href')||'';const isWa=href.indexOf('https://wa.me/')===0;const run=(e)=>{if(isWa){e.preventDefault();openContactMenu(href)}};a.addEventListener('pointerdown',run);a.addEventListener('click',run)})}
function init(){initLangToggle();initTTS();initMedia();initSearch();initSettings()}
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',()=>{init();initContact()})}else{init();initContact()}
